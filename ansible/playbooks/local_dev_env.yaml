- name: Import Autoware Universe Dev Environment
  ansible.builtin.import_playbook: autoware.dev_env.universe

- name: Configure Local Development Environment
  hosts: localhost
  connection: local
  roles:
    - role: autoware.ecu_system_setup.pacmod
      when: rosdistro == 'humble'
