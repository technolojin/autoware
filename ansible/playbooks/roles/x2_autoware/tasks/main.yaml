- name: Copy scripts
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: /opt/autoware/services/autoware/
    mode: 0755
  with_items:
    - run.sh
  become: true

- name: Copy service
  ansible.builtin.copy:
    src: autoware.service
    dest: /etc/systemd/system/
    mode: 0755
  become: true

- name: Enable service
  ansible.builtin.systemd:
    name: autoware.service
    daemon_reload: "{{ daemon_reload }}"
    enabled: true
    masked: false
  become: true
