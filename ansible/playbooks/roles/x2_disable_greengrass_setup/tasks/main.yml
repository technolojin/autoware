- name: Disable greengrass v2 setup timer
  ansible.builtin.systemd:
    name: greengrass-v2-setup.timer
    daemon_reload: "{{ daemon_reload }}"
    enabled: false
    masked: true
    state: stopped
  become: true
